export interface IDivision {
  name: string;
  slug: string;
  thumbnail?: string;
  description?: string;
}

// Division name: Dhaka Division
// slug => dhaka-division
// /:id => /78e65dre5dfasd
// /:id =>
// /:slug => /division/dhaka-division
